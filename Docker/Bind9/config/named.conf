acl internal {
    <ipAddress>/<netMask>;
    <ipAddress>/<netMask>;
    <ipAddress>/<netMask>;
};


options {
    forwarders {
        8.8.8.8;
        1.1.1.1;
    };
    allow-query { internal; };
};

zone "exempleDomain" IN {
    type master;
    file "/etc/bind/exempleDomain.zone";
};